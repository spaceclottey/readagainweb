import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'
import { useState, useEffect } from 'react'

export default function Add() {

  const [link, setLink] = useState("https://lesswrong.com")
  const [text, setText] = useState("<marquee> <strong> Hello </strong>   </marquee>")
  const [buttonPress, setButtonPress] = useState(false)

  useEffect(() => {
    setButtonPress(false)
    async function fetchThatData(){
      console.log("link inside thingy: ", link)
      const hello = await fetch("/api/fetchText", {
          method: "POST",
          body: JSON.stringify({"link": link}),
        })
      const text = await hello.text()
      setText(text)
    }
    fetchThatData()
  }, [buttonPress])

  const handleSubmit = (e) => {
    e.preventDefault()
    setButtonPress(true)
  }



  // // const getArticle = async () => {
  // //   const pageText = await fetch("/api/fetchText", {
  // //     method: "POST",
  // //     body: JSON.stringify({"link": link}),
  // //   })
    
  // //   // .then((response) => response)
  // //   // .then((responseData) => {
  // //   //   console.log("Response Data")
  // //   //   console.log(responseData)
  // //   //   console.log("Response Data.text")
  // //   //   console.log(responseData.text());
  // //   //   return responseData;
  // //   // })


  // //   console.log("Page text")

  // //   let awaited = await pageText
  // //   console.log(awaited)


  // //   // setText(fetchedText)

    


  // //   // const response = await fetch(link);
  // //   // if (response.ok) {
  // //   //   var json = await response.json();
  // //   // } else {
  // //   //   console.log("No response");
  // //   //   alert("HTTP-Error: " + response.status);
  // //   // }
  // //   // console.log(json)

  
  // // }

  


  const updateLink = (e) => {
    setLink(e.target.value);
  };

  return (
    <div className={styles.container}>
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>



      <main className={styles.main}>
        {/* <div dangerouslySetInnerHTML={{ __html: text}}></div> */}
        <div> 
          <input
            type="text"
            placeholder="https://lesswrong.com/..."
            name="link"
            className="w-3/4 py-1 px-2 text-sm rounded bg-gray-200"
            value={link}
            onChange={(e) => updateLink(e)}
          />
          {/* <button onClick={getArticle}> Add article </button> */}
          <button onClick={() => setButtonPress(true)}> Add article </button>
          <p> {link} </p> 
        </div>  
      </main>

      <footer className={styles.footer}>
        <a
          href="https://vercel.com?utm_source=create-next-app&utm_medium=default-template&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          Powered by{' '}
          <span className={styles.logo}>
            <Image src="/vercel.svg" alt="Vercel Logo" width={72} height={16} />
          </span>
        </a>
      </footer>
    </div>
  )
}
